<%= form_for [:admin, @work_type], :html => { :class => 'form-inline' } do |f| %>
  <% if @work_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_type.errors.count, "error") %> prohibited this work_type from being saved:</h2>

      <ul>
      <% @work_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
      <%= f.label :name %><br>
    <%= f.text_field :name %>
    </div>
  <div class="field">
      <%= f.label :description %><br>
    <%= f.text_area :description %>
    </div>
  <div class="field">
      <%= f.label :is_active %><br>
    <%= f.check_box :is_active %>
    </div>  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
