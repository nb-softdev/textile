<h1>Listing company_layouts</h1>

<table class="table">
  <thead>
    <tr>
      <th>Company</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @company_layouts.each do |company_layout| %>
      <tr>
        <td><%= company_layout.company.name %></td>

        <td><%= link_to 'Show', [:admin, company_layout] %></td>
        <td><%= link_to 'Edit', edit_admin_company_layout_path(company_layout) %></td>
        <td><%= link_to 'Destroy', [:admin, company_layout], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Company layout', new_admin_company_layout_path %>
